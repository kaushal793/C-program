#include<stdio.h>

int main()
{
  // int i,n;
  // int Sum=0,num;
  // float Average;

  // printf("\n Enter the nth element:-\n");
  // scanf("%d",&n);



  
  // for(i=0;i<=n;++i)
  //  {
    
  //    Sum = Sum +i;
  //  }

  // Average = (Sum*1.00)/n;

  // printf("Sum of the 1 to %dth number is %d",n, Sum);
  // printf("\nAverage of the 1 to %d number is %.2f",n, Average);
 

  

  return 0;
}